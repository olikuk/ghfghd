apiVersion: v1
kind: Service
metadata:
  name: gjfgedv
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gjfgedv"
    port: 8080
  selector:
    app: gjfgedv
